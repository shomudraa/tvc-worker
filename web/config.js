// Where the worker (FFmpeg + face swap API) runs. Leave empty when the worker
// serves this site itself (npm run dev). Set to your Railway URL when the site
// is hosted separately on Vercel, e.g. "https://face-swap-worker.up.railway.app"
window.WORKER_URL = "";
