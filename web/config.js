// Where the worker (FFmpeg + face swap API) runs. Leave empty when the worker
// serves this site itself (npm run dev). Set to your Render URL when the site
// is hosted separately on Vercel, e.g. "https://tvc-worker.onrender.com"
window.WORKER_URL = "";
